curl -k -X GET 'https://sonar.ndev.coic.mil/api/issues/search?ps=500&p=1&componentKeys=<PROJECT KEY>' -H 'api_key: <INSERT API KEY FROM SONARQUBE>' | jq -r '(.issues[] | [.key, .rule, .severity, .component, .project, .line, .hash, .status, .message, .effort, .debt, .author, .creationDate, .updateDate, .type, .organization]) | @csv' >> issues_udop.csv
